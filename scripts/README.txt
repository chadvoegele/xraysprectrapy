For scripts that depend on xrayspectrapy library, you will need to make sure
that the xrayspectrapy directory is in python's search path.

You can append to the search path by setting the PYTHONPATH environment
variable.

export PYTHONPATH=~/code/xrayspectrapy
